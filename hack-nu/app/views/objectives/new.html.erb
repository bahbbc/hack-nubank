<h1 class="titulo-principal" style="text-align:center; font-size:3em">Qual seu maior sonho?</h1>

<div class="row">
  <div class="col-xs-12 text-center">
    <%= form_for(@objective) do |f| %>
      <div class="btn-group-vertical" role="group" aria-label="...">
        <%= submit_tag 'Viajar', name: 'travel', class: 'btn btn-default navbar-btn' %>
        <%= submit_tag 'Comprar um carro', name: 'car', class: 'btn btn-default navbar-btn' %>
        <%= submit_tag 'Comprar um imóvel', name: 'house', class: 'btn btn-default navbar-btn' %>
        <%= submit_tag 'Fazer faculdade', name: 'faculty', class: 'btn btn-default navbar-btn' %>
        <%= submit_tag 'Outro', class: 'btn btn-default navbar-btn' %>
      </div>
    <% end %>
  </div>
</div>
